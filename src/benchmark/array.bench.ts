// bench("ArrayOperator with number array", () => {
// 	return {} as ArrayOperator<number[]>;
// }).types([0, "instantiations"]);

// bench("ArrayOperator with string array", () => {
// 	return {} as ArrayOperator<string[]>;
// }).types([0, "instantiations"]);

// bench("ArrayOperator with object array", () => {
// 	return {} as ArrayOperator<{ id: number; name: string }[]>;
// }).types([0, "instantiations"]);

// bench("ArrayOperator with nested object array", () => {
// 	return {} as ArrayOperator<{ user: { name: string; age: number } }[]>;
// }).types([0, "instantiations"]);

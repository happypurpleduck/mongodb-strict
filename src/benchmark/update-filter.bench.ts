// bench("UpdateFilter with simple object", () => {
// 	return {} as UpdateFilter<{ a: number; b: string }>;
// }).types([0, "instantiations"]);

// bench("UpdateFilter with nested object", () => {
// 	return {} as UpdateFilter<{ a: { b: number } }>;
// }).types([0, "instantiations"]);

// bench("UpdateFilter with arrays", () => {
// 	return {} as UpdateFilter<{ a: number[] }>;
// }).types([0, "instantiations"]);

// bench("UpdateFilter with complex schema", () => {
// 	return {} as UpdateFilter<{ a: number; b: string[]; c: { d: boolean } }>;
// }).types([0, "instantiations"]);
